<h1>Hello, {{this.name}}! Current timestamp is: {{this.timestamp}}</h1>

{{#each this.scope.items as |item|}}
  {{item.order}}
{{/each}}

<button type="button" {{on 'click' this.onButtonClick}} {{create-ref "button"}}>Click Me!</button>

{{this.page.wordCount}}